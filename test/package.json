{
  "name": "@reeg/test",
  "version": "0.0.0",
  "private": true,
  "description": "Cross-package integration and end-to-end tests, including the acceptance demo.",
  "type": "module",
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit",
    "live:checkpoint": "tsx live/checkpoint.ts",
    "live:acceptance": "tsx live/acceptance.ts"
  },
  "dependencies": {
    "@mysten/seal": "^1.1.3",
    "@mysten/sui": "^2.17.0",
    "@mysten/walrus": "^1.1.7",
    "@reeg/chain": "workspace:*",
    "@reeg/crypto": "workspace:*",
    "@reeg/sdk": "workspace:*",
    "@reeg/storage": "workspace:*",
    "@reeg/verify": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.22.4",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  }
}
